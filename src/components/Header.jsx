import styled from 'styled-components'

const Header = styled.header`
  padding: 20px;
  background: black;
  color: white;
  display: flex;
  flex-direction: row;

  img {
    padding: 15px;
  }
`

export default Header
